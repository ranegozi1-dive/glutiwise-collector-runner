name: MVP Bootstrap Stores (2 networks, up to 30)

on:
  workflow_dispatch:

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare folders
        run: |
          mkdir -p dumps

      - name: Pull scraper image
        run: |
          docker pull erlichsefi/israeli-supermarket-scarpers:latest

      - name: Download STORE files (RAMI_LEVY + SHUFERSAL)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}/dumps:/usr/src/app/dumps" \
            -e ENABLED_SCRAPERS="RAMI_LEVY,SHUFERSAL" \
            -e ENABLED_FILE_TYPES="STORE_FILE" \
            -e LIMIT=500 \
            erlichsefi/israeli-supermarket-scarpers:latest

      - name: Build stores payload (up to 30) and POST to Make
        env:
          MAKE_WEBHOOK_URL: ${{ secrets.MAKE_WEBHOOK_URL }}
          MAKE_APIKEY: ${{ secrets.MAKE_APIKEY }}
        run: |
          python3 - << 'PY' | curl -sS -X POST "$MAKE_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -H "x-make-apikey: $MAKE_APIKEY" \
              --data-binary @-
          import os, json, glob, re
          import xml.etree.ElementTree as ET
          from datetime import datetime, timezone

          # MVP: שתי רשתות, עד 30 סניפים. עדיפות לאזור הוד השרון/הסביבה, ואם אין מספיק — משלים אוטומטית.
          TARGET_TOKENS = [
              "הוד השרון", "כפר סבא", "רעננה", "הרצליה",
              "רמת השרון", "פתח תקווה", "תל אביב", "אחוזה"
          ]

          # ChainID -> retailer (לפי הקבצים שכבר ראית)
          CHAINID_TO_RETAILER = {
              "7290027600007": "ramilevy",
              "7290058140886": "shufersal",
          }

          # כדי שה-Route A לא ייפול על FK במחירים: נדאג שה-store_key הזה תמיד יופיע ב-stores שנשלחים
          REQUIRED_STORE_KEYS = ["ramilevy:001"]

          def norm(s: str) -> str:
              return re.sub(r"\s+", " ", (s or "").strip())

          def localname(tag: str) -> str:
              return tag.split("}")[-1] if "}" in tag else tag

          def looks_numeric(s: str) -> bool:
              return bool(re.fullmatch(r"\d{3,8}", (s or "").strip()))

          def find_first_text_anywhere(root, wanted_lower: str) -> str:
              for el in root.iter():
                  if localname(el.tag).lower() == wanted_lower and (el.text or "").strip():
                      return norm(el.text)
              return ""

          def text_in_subtree(elem, wanted_lower_set):
              for x in elem.iter():
                  if localname(x.tag).lower() in wanted_lower_set and (x.text or "").strip():
                      return norm(x.text)
              return ""

          def retailer_from_chain(chain_id: str, chain_name: str):
              if chain_id and chain_id in CHAINID_TO_RETAILER:
                  return CHAINID_TO_RETAILER[chain_id]
              cn = chain_name or ""
              if "שופרסל" in cn:
                  return "shufersal"
              if "רמי" in cn:
                  return "ramilevy"
              return None

          def parse_store_file(path):
              root = ET.parse(path).getroot()
              chain_id = find_first_text_anywhere(root, "chainid")
              chain_name = find_first_text_anywhere(root, "chainname")

              stores = []
              for el in root.iter():
                  if localname(el.tag).lower() != "store":
                      continue
                  store_id = text_in_subtree(el, {"storeid"})
                  if not store_id:
                      continue
                  city = text_in_subtree(el, {"city"})
                  address = text_in_subtree(el, {"address"})
                  name = text_in_subtree(el, {"storename"})
                  stores.append((store_id, city, address, name))
              return chain_id, chain_name, stores

          # איסוף מועמדים
          candidates = {"ramilevy": [], "shufersal": []}
          for path in glob.glob("dumps/**/*.xml", recursive=True):
              base = os.path.basename(path).lower()
              if not base.startswith("stores"):
                  continue
              chain_id, chain_name, stores = parse_store_file(path)
              retailer = retailer_from_chain(chain_id, chain_name)
              if retailer in candidates and stores:
                  candidates[retailer].extend(stores)

          if not candidates["ramilevy"] and not candidates["shufersal"]:
              raise SystemExit("No parsable STORE records found in dumps.")

          # בחירה: עד 15 לכל רשת (סה״כ עד 30). קודם לפי טוקנים באזור, ואז משלים.
          def score(city, address, name):
              city_for_search = "" if looks_numeric(city) else (city or "")
              hay = norm(f"{city_for_search} {address or ''} {name or ''}")
              return any(tok in hay for tok in TARGET_TOKENS)

          def build_selected_for(retailer, limit):
              items = candidates.get(retailer, [])
              preferred = []
              fallback = []
              for store_id, city, address, name in items:
                  (preferred if score(city, address, name) else fallback).append((store_id, city, address, name))
              out = preferred[:limit]
              if len(out) < limit:
                  out.extend(fallback[: (limit - len(out))])
              return out

          per_retailer = 15
          sel_rami = build_selected_for("ramilevy", per_retailer) if candidates["ramilevy"] else []
          sel_shuf = build_selected_for("shufersal", per_retailer) if candidates["shufersal"] else []

          # אם אחת הרשתות לא זמינה, ממלאים עד 30 מהשנייה
          combined_raw = [("ramilevy", *x) for x in sel_rami] + [("shufersal", *x) for x in sel_shuf]
          if len(combined_raw) < 30:
              for retailer in ("ramilevy", "shufersal"):
                  for store_id, city, address, name in candidates.get(retailer, []):
                      combined_raw.append((retailer, store_id, city, address, name))
                      if len(combined_raw) >= 30:
                          break
                  if len(combined_raw) >= 30:
                      break

          selected = []
          seen_keys = set()

          def add_store_obj(retailer, store_id, city, address, name):
              store_key = f"{retailer}:{store_id}"
              if store_key in seen_keys:
                  return
              seen_keys.add(store_key)
              selected.append({
                  "retailer": retailer,
                  "store_id": str(store_id),
                  "store_key": store_key,
                  "store_name": name or "",
                  "city": city or "",
                  "address": address or "",
                  "area": "",
                  "store_type": ""
              })

          for retailer, store_id, city, address, name in combined_raw:
              add_store_obj(retailer, store_id, city, address, name)
              if len(selected) >= 30:
                  break

          # Force include REQUIRED_STORE_KEYS (so the minimal record won't fail FK)
          for sk in REQUIRED_STORE_KEYS:
              if sk not in seen_keys:
                  r, sid = sk.split(":", 1)
                  add_store_obj(r, sid, "", "", "")

          now = datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z")

          payload = {
              "source": "mvp_bootstrap_stores",
              "generated_at": now,
              "stores": selected[:30],
              "records": [{
                  "price_key": "7290000000011:ramilevy:001",
                  "barcode": "7290000000011",
                  "store_key": "ramilevy:001",
                  "price": 9.9,
                  "updated_at": now
              }]
          }

          print(json.dumps(payload, ensure_ascii=False))
          PY
